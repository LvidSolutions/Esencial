// Compatibility export for older documentation/import tooling. The Studio schema is
// authoritative so this path cannot silently drift into a second content model.
export {projectType as projectSchema} from '../studio/schemaTypes/projectType'
